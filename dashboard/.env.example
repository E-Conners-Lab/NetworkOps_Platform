# Dashboard Environment Variables
# Copy this file to .env and update values as needed

# API Server URL (used by React frontend)
REACT_APP_API_URL=http://localhost:5001

# WebSocket URL (defaults to API URL if not set)
REACT_APP_WS_URL=http://localhost:5001

# Allowed origin for CORS (backend)
DASHBOARD_ORIGIN=http://localhost:3000

# Flask configuration
FLASK_ENV=development
FLASK_DEBUG=0

# =============================================================================
# JWT Authentication
# =============================================================================
# IMPORTANT: Change this in production! Generate with: python -c "import secrets; print(secrets.token_hex(32))"
JWT_SECRET=your-secret-key-change-in-production

# Token expiration (hours)
JWT_EXPIRATION_HOURS=24

# User accounts (comma-separated, format: username:bcrypt_hash:role)
# Generate password hash with: python -c "import bcrypt; print(bcrypt.hashpw(b'yourpassword', bcrypt.gensalt()).decode())"
# Example: DASHBOARD_USERS=admin:$2b$12$...:admin,operator:$2b$12$...:operator
# Leave unset to use default dev users (admin/admin, operator/operator)
# DASHBOARD_USERS=
